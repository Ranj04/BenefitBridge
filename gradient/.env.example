# Copy to .env (gitignored) or export in your shell. Never commit real values.

# DigitalOcean API token with GenAI (Gradient) read/write scopes
DO_API_TOKEN=

# Person B's live /screen URL (App Platform). Until it exists, use the local mock:
SCREEN_URL=http://localhost:8787/screen
