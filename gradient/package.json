{
  "name": "benefitbridge-gradient",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Person A: DigitalOcean Gradient AI graph (intake, router, food agent, KB, guardrails) for BenefitBridge",
  "scripts": {
    "mock": "tsx src/mock-screen.ts",
    "list": "tsx src/list-resources.ts",
    "typecheck": "tsc --noEmit --pretty false 2>&1 | grep -v 'node_modules/' | grep 'error TS' && exit 1 || exit 0",
    "test": "vitest run",
    "provision": "tsx src/provision.ts"
  },
  "dependencies": {
    "@digitalocean/dots": "^1.18.0",
    "@microsoft/kiota-http-fetchlibrary": "^1.0.0-preview.103",
    "fastify": "^4.28.1"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.19.0",
    "typescript": "^7.0.2",
    "vitest": "^2.1.0"
  }
}
